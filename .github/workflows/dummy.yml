name: Chained Workflow
on:
  workflow_run:
    workflows: ["Process PR to Dev"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: echo "Deploying!"
